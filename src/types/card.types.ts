export interface CardData {
    name: string;
    imageUrl: string;
    rank: number;
  }
  
  export interface CardCount {
    rank: number;
    count: number;
    originalCount: number;
    name: string;
    imageUrl: string;
  }